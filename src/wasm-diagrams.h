#include <map>
#include <string>

void drawChartToCanvas(std::string       &canvasName,
                       std::map<int, int> data,
                       unsigned           width,
                       unsigned           height,
                       std::string       &fillStyle);
