#!/bin/bash
mkdir -p tmp
for i in $@ ; do
	inkscape -w $i -h $i logo.svg -o tmp/$i.png &
	TMP="$TMP tmp/$i.png"
done
wait
convert $TMP logo.ico
for i in $@ ; do
        rm -f tmp/$i.png
        echo -n "${i}x$i "
done
echo
